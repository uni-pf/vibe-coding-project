# PRD · 3D 探索房间

> **项目工作名**：3D 探索房间（正式名待定）
> **作者**：uni-pf ｜ **仓库**：vibe-coding-project
> **版本**：v1.0 ｜ **日期**：Day 4 · 第 1 周
> **上游文档**：`research.md`（Day 3 · 需求研究）
> **下游文档**：技术选型（Day 5）→ 开发（Day 6 起）

---

## 1. 项目概述

### 1.1 一句话定义

一间可以用鼠标探索的 3D 房间：房间里有 3 件属于同一个虚构角色的物件，用户点击物件打开一页配有文字与画面的内容，三件物件的回忆串起来，构成一段完整的叙事。

### 1.2 为什么做

给作品集访客（招聘方、同行、潜在合作方）看一个东西，说明**我能独立做出一个强引导的 3D 交互叙事作品，而且不靠堆特效**。

判断标准不是「特效多厉害」，而是访客看完后的感受：**这个人能把 3D 和文字叙事捏在一起。**

### 1.3 目标用户

| 用户 | 他们关心什么 | 对设计的要求 |
| --- | --- | --- |
| 招聘方 / 面试官 | 3 分钟内判断能力边界 | 首屏要能立刻看懂「这是什么」；不能有学习成本 |
| 同行开发者 | 实现方式与完成度 | 交互要经得起反复点击、反复进出内容页而不错乱 |
| 潜在合作方 | 审美与表达力 | 视觉风格统一，叙事完整，不出现「半成品感」 |

### 1.4 用户的核心场景

> 访客打开链接，看到一个昏暗但有几处微光的房间。他动了动鼠标，视角随之平移；某件物体在发光。点下去，房间轻微推进，画面变暗，一页文字和一张图浮出来，讲了一段回忆。看完返回房间，他发现还有别处也在发光。

### 1.5 内容设定：虚构角色（待补充 · 未完成项）

- 角色为**虚构人物**，不映射任何真实个人经历，不涉及隐私内容
- 需要一份**角色设定**（姓名、年代、处境、三件物件分别属于他/她的哪一段），作为内容页配文的写作依据
- 角色设定**本期只需写到「够三件物件用」的程度**，不写完整世界观

> ⚠️ **状态：未定，后续补充。**
> 本条为当前 PRD 的已知缺口，不是遗漏。**角色设定稿是 F4.2（配文撰写）与 A5（叙事可串通）的前置依赖**，
> 必须在撰写配文之前完成，否则 A5 无法验收。写作口径：够三件物件用即可，不做完整世界观。

---

## 2. 用户流程

### 2.1 主流程（一次完整访问）

```
打开页面
  ↓
[1] 加载 3D 场景（有加载进度反馈）
  ↓
[2] 房间出现：轻微的环境动效（尘埃、光影），视角随鼠标轻微平移
  ↓
[3] 用户识别出「有东西在发光」
  ↓
[4] 点击发光物件 → 场景推进/转场（电影式过渡，非硬切）
  ↓
[5] 内容页：配文 + 画面，讲一段回忆
  ↓
[6] 返回房间（保留已解锁状态）
  ↓
[7] 重复 [3]–[6]，直到 3 件物件全部看完
  ↓
[8] 全部解锁后 → 收束信号（收尾内容 / 明确的「看完了」状态）
```

### 2.2 软引导下的叙事顺序保障

解锁机制为**软引导**（物件自发光吸引，顺序自由）。因为没有硬性门锁，叙事顺序必须由**内容本身**承担：

1. **物件强度递进**：三件物件的发光强度/视觉吸引力递增，形成自然的注意顺序
2. **配文互指**：第 2 件的配文里回指第 1 件，第 3 件回指前两件
3. **收束信号**：只有三件全部解锁，才出现收尾内容——保证「看完」是一个明确状态，而不是用户自己猜

> 结论：**顺序自由，但叙事有锚点。** 用户以任意顺序进入，读完三件后仍能拼出完整时间线。

### 2.3 异常路径（必须处理）

| 情况 | 期望表现 |
| --- | --- |
| 加载失败 / 资源缺失 | 显示明确的错误提示与重试入口，**不允许出现纯黑屏** |
| 用户点击非交互区域 | 无反应，不出现报错、不出现误触发 |
| 用户在内容页反复进出 | 房间视角、物件状态、解锁进度均保持一致，不重置、不错乱 |
| 用户中途离开再回来（刷新） | 解锁进度丢失可接受，但必须回到可正常使用的初始态 |

---

## 3. 功能清单

按「本期必须做完」组织，每项标注完成程度。

### F1 · 3D 房间场景（本期核心）

- F1.1 一个室内场景，包含地面、墙面、基础光照，风格统一
- F1.2 鼠标移动驱动视角轻微平移（视差感），幅度克制，不引起眩晕
- F1.3 环境氛围：至少一种持续的环境动效（如尘埃浮动 / 光影变化），让空间「不死」
- F1.4 加载过程有进度反馈，加载期间不出现空白页

### F2 · 可交互物件（本期核心，目标 3 件）

- F2.1 物件在场景中的位置、大小、视觉形态与角色设定一致
- F2.2 未解锁物件具有**自发光的视觉提示**（不依赖文字教程）
- F2.3 鼠标悬停时有反馈（如高亮/微缩放）
- F2.4 点击命中判定准确（不出现「点了没反应」或「点错物件」）
- F2.5 已解锁物件在视觉上与未解锁可区分（保留状态）

### F3 · 物件 → 内容页转场（本期核心）

- F3.1 点击后触发电影式过渡（镜头推进 + 淡出/淡入），**禁止硬切**
- F3.2 过渡时长与缓动统一，三件物件使用同一套过渡参数
- F3.3 返回房间时同样有过渡，且返回后视角回到合理位置

### F4 · 内容页

- F4.1 页面结构：标题 + 配文 + 至少一张画面素材
- F4.2 配文内容基于虚构角色设定撰写，三篇之间形成互指
- F4.3 滚动阅读正常（**基础滚动即可，不做滚动驱动的额外动效**）
- F4.4 提供明确的返回入口

### F5 · 完成状态与收束

- F5.1 记录解锁进度（当前会话内有效即可）
- F5.2 三件全部解锁后，出现明确的收尾内容或收束信号

### F6 · 基础可用性

- F6.1 桌面端主流浏览器（Chrome / Edge）正常使用
- F6.2 窄屏（375px）下不出现横向滚动条；3D 场景在移动端**降级为可接受的表现或明确提示**
- F6.3 全站视觉风格统一，与既有 UI 基调一致（主色 `#185FA5`）

### 可扩展性要求（对应「留出可扩展空间」）

- 场景支持从 3 件增加到 5 件物件时，**不改场景基础架构、不改已有物件的交互代码**，只新增物件配置与内容页
- 物件与其内容的对应关系以**数据配置**形式组织，不写死在逻辑里

---

## 4. 本期不做

> 本节是本次 PRD 的核心。每一项都记录「砍了什么 / 为什么砍 / 代价是什么 / 什么条件下捡回来」。

### D1 · 真实空间扫描 + 高斯泼溅重建

- **理由**：需要 LiDAR 硬件与专业重建流程，属另一条技术路线，与「虚构角色叙事」这个已定方向冲突
- **代价**：放弃真实现场的沉浸感，场景需自行构建
- **捡回来的条件**：项目方向改为「记录真实空间」时

### D2 · 角色控制器与物理系统

- **理由**：参照 Grand Theater 最重的部分（约 150 个物理物件、找方向约 2 年），且与「点击物件读回忆」的核心叙事无关
- **代价**：用户不能自由行走，探索感依赖视角平移而非位移
- **捡回来的条件**：本期交付完成、且有充裕额外时间时

### D3 · 多房间 / 多场景

- **理由**：一个房间足够承载 3 件物件的叙事，还能省掉导航、地图、寻路
- **代价**：空间规模有限，叙事容量上限受限
- **捡回来的条件**：单房间已完整交付且内容确需扩展时

### D4 · VR / WebXR

- **理由**：目标用户是浏览器里的作品集访客，VR 不带来叙事增量，反而抬高使用门槛
- **代价**：放弃沉浸式头显体验
- **捡回来的条件**：不作为作品集方向考虑（长期也不做）

### D5 · 自定义着色器与后处理特效

- **理由**：属锦上添花，不在最小可用版本范围内；先把交互与叙事做扎实
- **代价**：视觉表现依赖基础材质与光照，画面不会「惊艳」
- **捡回来的条件**：本期交付后作为独立迭代项

### D6 · 空间音频

- **理由**：位置音效对「点击读内容」这种低频交互收益低，实现成本不低
- **代价**：氛围沉浸感减弱
- **捡回来的条件**：只保留可选背景音；位置音效长期不做

### 明确「保留」项（回应砍功能讨论）

- **滚动视差动效保留**：内容页的滚动视差按要求**不砍**，保留在 F4.3 范围内（当前记为「基础滚动 + 视差动效」，实现程度在 Day 5 技术选型中定档）

---

## 5. 验收标准

> 每条都必须能被「是 / 否」判定。写在括号里的是验证方式。

### 5.1 功能验收

| # | 标准 | 验证方式 |
| --- | --- | --- |
| A1 | 首页打开后能看到 3D 房间场景，并能用鼠标移动驱动视角平移 | 肉眼观察 + 操作 |
| A2 | 房间里存在 3 件可交互物件，且这 3 件在未解锁时均有可见的自发光提示 | 肉眼观察 |
| A3 | 每件物件点击后都能打开对应内容页，且内容页包含标题、配文、至少一张画面素材 | 逐件点击核对 |
| A4 | 点击 → 内容页、返回 → 房间，两个方向均为过渡效果，无硬切 | 录屏核对；过渡时长 ≥ 300ms，且画面包含透明度或位置的变化 |
| A5 | 3 篇配文可串成一条完整时间线，第 2、3 篇分别回指前文 | 通读三篇配文 |
| A6 | 3 件全部解锁后，出现明确的收尾内容或收束信号 | 全解锁后观察 |
| A7 | 已解锁物件与未解锁物件在视觉上可区分 | 解锁前后对比 |

### 5.2 体验与性能验收

| # | 标准 | 验证方式 |
| --- | --- | --- |
| B1 | 桌面端（Chrome）3D 场景可交互时间 ≤ 3 秒 | 浏览器 Performance 面板 |
| B2 | 交互帧率稳定 ≥ 30 FPS，不出现持续 3 帧以上卡顿 | Chrome FPS 计数器 |
| B3 | 首屏无任何文字教程提示（不含「点击这里」「拖动查看」类引导语） | 肉眼观察 |
| B4 | 首次进入的用户在 10 秒内能识别出「有物件可以点击」 | 找 1 名未看过的人实测 |
| B5 | 点击命中准确：不会出现点了没反应，也不会误触发相邻物件 | 反复点击测试 |

### 5.3 稳定性与异常验收

| # | 标准 | 验证方式 |
| --- | --- | --- |
| C1 | 加载失败时显示明确错误提示与重试入口，不出现纯黑屏 | 断网/改错资源路径模拟 |
| C2 | 内容页反复进出 5 次后，房间视角与解锁进度保持一致，不重置不错乱 | 反复操作测试 |
| C3 | 刷新页面后能回到可正常使用的初始态 | 刷新观察 |
| C4 | 点击非交互区域无任何报错与误触发 | 随机点击测试 |

### 5.4 响应式验收

| # | 标准 | 验证方式 |
| --- | --- | --- |
| D1 | 375px 宽度下不出现横向滚动条 | 浏览器设备模拟 |
| D2 | 375px 宽度下可点击元素热区 ≥ 44×44px | 设备模拟下测量 |
| D3 | 移动端 3D 场景有明确降级表现或提示，不出现卡死 | 真机或设备模拟 |

### 5.5 可扩展性验收

| # | 标准 | 验证方式 |
| --- | --- | --- |
| E1 | 新增第 4、5 件物件时，不需修改场景基础架构与已有物件的交互代码 | 代码审查 + 实际新增一次 |
| E2 | 物件与其内容的对应关系以数据配置形式存在，不写死在逻辑中 | 代码审查 |

---

## 6. 风险与假设

### 6.1 当前假设

| # | 假设 | 若假设不成立的后果 |
| --- | --- | --- |
| H1 | 纯静态部署（无后端）足以满足全部需求 | 需引入服务端，进度记录等需求要重新设计 |
| H2 | 内容页为 DOM 页面，与 3D 场景分离 | 若改为 3D 内渲染文字，工作量与可读性均恶化 |
| H3 | 3D 素材可自行制作或使用免费授权资源 | 素材获取受阻将直接阻塞 F1/F2 |
| H4 | 技术栈尚未确定，以 Day 5 结论为准 | 选型若强依赖构建工具，则部署方式需同步调整 |

### 6.2 主要风险

| # | 风险 | 影响 | 应对 |
| --- | --- | --- | --- |
| R1 | 3D 场景性能不达标（B1/B2 未过） | 验收失败 | 控制模型面数与贴图体积；必要时减少环境动效 |
| R2 | 电影式过渡（F3）实现难度高于预期 | 核心体验缺失 | 先做可用的淡入淡出，视觉升级后置 |
| R3 | 虚构角色设定写不充分，配文空洞 | A5 无法验收 | 先写设定再写配文，设定稿单独产出 |
| R4 | 移动端表现不可接受 | D1–D3 未过 | 明确降级策略，宁可提示不硬撑 |
| R5 | 范围膨胀（软引导下想加更多物件） | 无法按时交付 | 严格卡在本期 3 件；第 4、5 件属下一迭代 |

---

## 附录：与上游文档的对应关系

| research.md 结论 | 在本 PRD 中的落点 |
| --- | --- |
| 3D 负责探索、DOM 负责阅读与视差 | §2.1 流程 [4][5]、§3 F3/F4 |
| 回忆应「按顺序解锁」 | §2.2（改为软引导 + 内容锚点） |
| 不写教程，靠试错与自发光吸引 | §3 F2.2、§5 B3/B4 |
| 场景与页面之间需电影式过渡 | §3 F3 |
| 「本期不做」6 项 | §4 D1–D6 |
